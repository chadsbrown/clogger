[package]
name = "logger-core"
version = "0.1.0"
edition = "2024"

[dependencies]
contest-engine = { git = "https://github.com/chadsbrown/contest-engine" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
