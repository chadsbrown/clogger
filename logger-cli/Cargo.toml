[package]
name = "logger-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
logger-core = { path = "../logger-core" }
